using System.Numerics;
using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
// using System.Numerics;
// using Random=UnityEngine.Random;
public class coinScript : MonoBehaviour
{
   public GameObject coinImg;
   public Transform coinGenPoint;
    float distance;

    // // Start is called before the first frame update
    void Start()
    {
      
     
    }

    // // Update is called once per frame
    void Update()
    {
        // distance = Random.Range(0,4);
        // if(transform.position.x < coinGenPoint.transform.position.x)
        // {
        //     transform.position = new Vector3(transform.position.x + Range(1,5), distance, transform.position.z);
        //     Instantiate(coinImg, transform.position.x , transform.rotation);
        // }
    }
}
